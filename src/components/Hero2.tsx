import { Flex, panda } from "../../styled-system/jsx";

export const Hero = () => {
  return (
    <Flex
      flexDirection="column"
      minHeight="min(calc(100vh - 9rem), 50rem)"
      bgColor="red"
    ></Flex>
  );
};
